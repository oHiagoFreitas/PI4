import React from 'react';
import '../../Style/AtletasTitle.css';

const RelatorioTitle = () => { // Altere o nome aqui
    return (
        <div className="AtletasTitleMessage">
            <h1>Relatorios</h1>
        </div>
    );
};

export default RelatorioTitle;
